apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-processor-service
  namespace: ns-review-processor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-processor-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: review-processor-service
    spec:
      serviceAccountName: sa-review-processor-service
      containers:
        - name: review-processor-service
          image: jsfelipearaujo/review-processor-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 4
            successThreshold: 1
          resources:
            limits:
              memory: 256Mi
              cpu: 100m
            requests:
              memory: 128Mi
              cpu: 100m
          envFrom:
            - configMapRef:
                name: review-processor-service-config
      terminationGracePeriodSeconds: 30
      nodeSelector: {}
      tolerations: []
